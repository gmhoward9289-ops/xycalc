# Arm 4 — ClickHouse MergeTree on real AWS S3 (2026-08-21)

**Status:** DONE + TEARDOWN=OK.

| | |
|---|---|
| Instance | `i-019a5e50fb4372399` (terminated) — `m6i.large`, us-east-2 |
| Stage | `tmp/xycalc-ch-s3-20260821f1858/` |
| Bucket | `xycalc-ch-s3-20260821-185845` (emptied + deleted) |
| IAM | `xycalc-ch-s3-xycalc-ch-s3-20260821f1858` (deleted) |
| Harness | `tools/bench/s3_stack/perf.sh` via `_aws_ch_s3_launch.sh` (`SKIP_MINIO=1`) |
| Window | `2026-08-21T23:00:06Z` → `23:02:49Z` |

## Proof it hit real S3 (not MinIO)

- Compose path: `compose.yml` + `compose.external-s3.yml`; RSS table has **no** `minio` row.
- `system.parts`: `disk_name=s3`, `rows=2000000`, `parts=1`.
- Pre-teardown `aws s3 ls --recursive --summarize`: **66 objects / 63,671,360 bytes**.

## Colocation RSS (docker stats)

| service | idle | loaded | under_load |
|---|---:|---:|---:|
| clickhouse | 143.7 MiB | 217.5 MiB | 247.7 MiB |
| mongo | 71.55 MiB | 518.4 MiB | 519.6 MiB |
| redis | 3.773 MiB | 3.777 MiB | 3.992 MiB |
| worker | 110.2 MiB | 110.5 MiB | 119.2 MiB |

- Mongo oversub: **2.02×** (dataSize 1086 MB / 0.5 GB WT cache, 1.5M docs).
- ClickHouse S3 scan during under_load: **0.29 s**.

## Note on results.json

Guest `perf.sh` wrote the live OUT, then an older launch helper did `cp results.json` and briefly replaced it with the packaged MinIO stub. Authoritative numbers are `results/perf.stdout.txt` + `perf.stderr.txt`; reconstructed payload is `results/s3_stack_results.live.json`. Launch helper fixed so future arms do not clobber OUT.

## Corpus

`data/observations/aws-ch-s3-2026-08-21.yaml` + source `obs-aws-ch-s3-2026-08-21`.
